#include "materialmanager.h"  // MaterialManager 클래스 헤더 파일 포함
#include <iostream>


int main() {
    MaterialManager manager;  // MaterialManager 객체 생성

    // 메인 메뉴를 표시하고 사용자에게서 입력을 받음
    manager.displayMenu();

    return 0;  // 프로그램 종료
}
